# Agent/session memory — local only, pre-populated with real setup state.
# These files are local continuity, compatibility, archive, or audit state.
.claude/
AGENTS.md
docs/PROJECT_STATE.md
docs/PROJECT_STATE-archive-*.md
docs/campaign-1/HISTORY.md
CLAUDE.md

# AI-security campaign MOVED to its own repo (2026-07-30):
# ~/Documents/github/ai-sec/ — a separate service that runs on the same box and
# shares this box's Qdrant + model server, but is NOT coupled to Audrey's pipeline.
# Nothing ai-security lives in this repo anymore; this note is a breadcrumb only.

# Lesson-side working docs that should not ship to GitHub.
# AUDIT.md collects code-review findings raised during lesson writing —
# it's an internal queue, not teaching material. One per course.
docs/lessons/AUDIT.md
docs/lesson-python/AUDIT.md
docs/lesson-python/_LESSON-*-HANDOFF.md

# OpenClaw builder's guide — laptop-local; references Unraid LAN IP
# and personal channel tokens. Reference doc for building agents.
docs/guides/openclaw-builders-guide.md

# Hermes reference — laptop-local working notes on the NousResearch
# Hermes Agent integration via passthrough; references LAN IP + OWUI.
docs/guides/hermes-reference.md

# Bot workspace tools — the build/deploy plans MOVED to the standalone
# bot-tools-mcp repo (2026-07-10): ~/Documents/github/bot-tools-mcp/docs/plans/
# (still gitignored there). Nothing bot-tools-plan-related lives in this repo
# anymore; these ignore lines are kept as a breadcrumb only.

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# uv
.uv/

# Environment / secrets
.env
.env.local
.env.*.local

# Runtime data (regenerable, should live in Unraid appdata on deploy)
data/
sandbox/
*.db
*.db-journal
*.sqlite
*.sqlite3

# Model / embedding caches (should live in Unraid checkpoints share on deploy)
.cache/
clip-cache/

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
logs/
