# Agent/session memory — local only, pre-populated with real setup state.
# These files are local continuity, compatibility, archive, or audit state.
.claude/
AGENTS.md
docs/PROJECT_STATE.md
docs/campaign-1/HISTORY.md
CLAUDE.md

# Lesson-side working docs that should not ship to GitHub.
# AUDIT.md collects code-review findings raised during lesson writing —
# it's an internal queue, not teaching material.
docs/lessons/AUDIT.md

# OpenClaw prototype plan — laptop-local; references Unraid LAN IP and
# a personal experiment with no place in the public repo.
docs/campaign-2/openclaw-prototype-plan.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# uv
.uv/

# Environment / secrets
.env
.env.local
.env.*.local

# Runtime data (regenerable, should live in Unraid appdata on deploy)
data/
sandbox/
*.db
*.db-journal
*.sqlite
*.sqlite3

# Model / embedding caches (should live in Unraid checkpoints share on deploy)
.cache/
clip-cache/

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
logs/
