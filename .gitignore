# Session memory — local only, pre-populated with real setup state
# HISTORY (formerly CONTINUITY) stays laptop-local; lesson docs ship to GitHub.
docs/campaign-1/HISTORY.md
CLAUDE.md

# Lesson-side working docs that should NOT ship to GitHub.
# AUDIT.md collects code-review findings raised during lesson writing —
# it's an internal queue, not teaching material.
docs/lessons/AUDIT.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# uv
.uv/

# Environment / secrets
.env
.env.local
.env.*.local

# Runtime data (regenerable, should live in Unraid appdata on deploy)
data/
sandbox/
*.db
*.db-journal
*.sqlite
*.sqlite3

# Model / embedding caches (should live in Unraid checkpoints share on deploy)
.cache/
clip-cache/

# Editor / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
logs/
